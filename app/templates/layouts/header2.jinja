<header class="navbar navbar-expand-md navbar-light d-print-none">
    <div class="container-xl">
        <h1 class="navbar-brand navbar-brand-autodark d-none-navbar-horizontal pe-0 pe-md-3">
            <a href="..">
                <img src="{{ url_for('static', filename= 'logo-bil-H.png') }}" alt="IDLAB" class="navbar-brand-image">
            </a>

        </h1>
        <div class="page-subtitle" color="blue"><b>{{title}}</b></div>

        <div class="navbar-nav flex-row order-md-last">
            <div class="nav-item d-none d-md-flex me-3">

            </div>
            <div class="d-none d-md-flex">

                <div class="nav-item dropdown">
                    <a href="#" class="nav-link d-flex lh-1 text-reset p-0 show" data-bs-toggle="dropdown"
                        aria-label="Open user menu" aria-expanded="false">
                        <span class="avatar avatar-sm"
                            style="background-image: url(https://api.dicebear.com/6.x/shapes/svg?seed={{ session['current_user']['github_profile']['login'] }})"></span>
                        <div class="d-none d-xl-block ps-2">
                            <div>{{ session['current_user']['github_profile']['name'] }}</div>
                            <div class="mt-1 small text-muted">{{ session['current_user']['github_profile']['login'] }}
                            </div>
                        </div>
                    </a>
                    <div class="dropdown-menu dropdown-menu-end dropdown-menu-arrow " data-bs-popper="static">
                        <a href="#" class="dropdown-item">Reports</a>
                        <div class="dropdown-divider"></div>
                        <a href="#" class="dropdown-item">Settings</a>
                        <a href="{{ url_for('auth.logout') }}" class="dropdown-item">Logout</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</header>