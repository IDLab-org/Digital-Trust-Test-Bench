<html lang="en">
{% include 'layouts/head.jinja' %}

<body class="d-flex flex-column">
    <div class="page page-center">
        <header class="navbar navbar-expand-md sticky-top d-print-none">
            <div class="container-xl">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar-menu" aria-controls="navbar-menu" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <h1 class="navbar-brand navbar-brand-autodark d-none-navbar-horizontal pe-0 pe-md-3">
                    <a href="{{ url_for('main.index') }}">
                    <img src="{{ url_for('static', filename='images/logo-bil-H.png') }}" width="110" height="32" class="navbar-brand-image">
                    </a>
                </h1>
                <div class="navbar-nav flex-row order-md-last">
                    {# <div class="nav-item dropdown d-none d-md-flex me-3">
                        {% include 'components/notifications.jinja' %}
                    </div> #}
                    {# <div class="nav-item d-none d-md-flex me-3">
                        <div class="btn-list">
                            <a href="{{ url_for('auth.logout') }}" class="btn btn-pill btn-sm">
                                <i class="bi bi-x-lg me-1"></i> Sign out
                            </a>
                        </div>
                    </div> #}
                    <div class="nav-item dropdown">
                        <a href="{{ url_for('main.index') }}" class="nav-link d-flex lh-1 text-reset p-0" data-bs-toggle="dropdown" aria-label="Open user menu" aria-expanded="false">
                            <span class="avatar avatar-sm" style="background-image: url(https://api.dicebear.com/6.x/shapes/svg?seed={{ session['user_info']['email'] }})"></span>
                            <div class="d-none d-xl-block ps-2">
                            <div>{{ session['user_info']['email'] }}</div>
                            <div class="mt-1 small text-secondary">{{ session['user_info']['organization'] }}</div>
                            </div>
                        </a>
                        <div class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                            {# <a href="#" class="dropdown-item">Status</a> #}
                            {# <a href="#" class="dropdown-item">Profile</a> #}
                            {# <a href="#" class="dropdown-item">Feedback</a> #}
                            {# <div class="dropdown-divider"></div> #}
                            {# <a href="#" class="dropdown-item">Settings</a> #}
                            <a href="{{ url_for('auth.logout') }}" class="dropdown-item">Logout</a>
                        </div>
                    </div>
                </div>
                {# <div class="collapse navbar-collapse" id="navbar-menu">
                    <div class="d-flex flex-column flex-md-row flex-fill align-items-stretch align-items-md-center">
                        <ul class="navbar-nav">
                            <div class="nav-item me-3">
                            </div>
                        </ul>
                    </div>
                </div> #}
            </div>
        </header>
        {# {% include "layouts/header.jinja" %} #}
        <div class="page-wrapper">
            {% block page %}{% endblock %}
            {% include 'layouts/footer.jinja' %}
        </div>
    </div>
    <script src="{{ url_for('static', filename='js/tabler.min.js') }}"></script>
    <script src="{{ url_for('static', filename='js/jquery.min.js') }}"></script>
</body>

</html>